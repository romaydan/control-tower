interface IPlane {
  flightNumber: string;
  hasLanded: boolean;
  hasMaintained: boolean;
  isLoaded: boolean;
  model: string;
  planeType: string;
  additionalInfo: number;
}
export default IPlane;
